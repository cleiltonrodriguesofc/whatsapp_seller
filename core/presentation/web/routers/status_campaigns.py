"""
Status campaign routes: list, create, edit, schedule and delete status campaigns.
Implemented in Phase 2 (feat/status-update branch).
"""
from fastapi import APIRouter

router = APIRouter(tags=["status"])
